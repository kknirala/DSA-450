// Find the Union and Intersection of the two sorted arrays.

let ar1 = [1,2,5,8,13];
let ar2 = [3,5,8,14,15,45,56];
let maxLen = ar1.length>ar2.length?ar1.length:ar2.length;
function findUnion(){
    let result = [];
    let j=0,k=0;
   for(let i =0;i<maxLen;i++){
       if(ar1[j]!null && ar1[j]<=ar2[k]){
           result.push(ar1[j]);
           j++;
       }
       else if(){

   }
    
    
    console.log(result);
}

function findIntersection(){
     let result = [];
     
     
    console.log(result);
}

findUnion();
findIntersection();
